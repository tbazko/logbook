import mockStore from 'redux-mock-store'
import * as action from '../actions'

describe('ActivityCalendar actions', () => {
  let store

  beforeEach(() => {
    store = mockStore()
  })

  it('', async () => {
  })
})
